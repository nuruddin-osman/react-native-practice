import { View, Text } from 'react-native';
import React from 'react';

const First = () => {
  return (
    <View>
      <Text style={{ fontSize: 30 }}>react native android emulator when</Text>
    </View>
  );
};

export default First;
